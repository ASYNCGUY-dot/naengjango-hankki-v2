# 냉장고 한끼 V3 — 착수 계획

작성일: 2026-08-12
전제: V2는 `v2.0.0` 태그로 종료. 진단 근거는 `PROJECT_REPORT_2026-08-12.md`.

## V3를 하는 이유 (사용자 정의)

1. 보고서의 문제점·개선사항 해결과 디벨롭
2. React로 전환
3. 실사용 테스트 전에 디자인 개편
4. 회원관리 및 로그인 데이터 개편
5. 런칭

이 다섯 개는 서로 독립적이지 않다. **5번(런칭)이 나머지 넷의 요구 수준을 전부
끌어올린다.** 그래서 아래 계획은 "런칭할 서비스"를 기준으로 짰다.

---

## 1. 다섯 항목에 대한 평가

### 2번 React 전환 — 근거가 충분하다

V2에서 Reflex가 만든 제약이 문서로 확인된 것만 다섯 가지다. 프론트 테스트 도구
부재(4,047줄 커버리지 0%), websocket 상시 프로세스 강제, 상태 락 만료로 인한 로그인
실패, 그로 인한 호스팅 3분할, 로컬 개발 중 연결 끊김 반복.

게다가 V2 백엔드는 **애초에 React 전환을 대비해 분리해둔 구조**다. FastAPI
엔드포인트 53개와 Supabase 데이터(레시피 1,148 / 영양DB 302,629행)를 그대로
재사용할 수 있으므로 버리는 자산이 적다.

**스코프 실측**: V2 프론트는 화면 컴포넌트 함수 81개, 이벤트 핸들러 67개,
State 클래스 2개다. **그리고 전체가 단일 페이지다** — 탭 전환을 State로만
처리하고 실제 라우트는 `/demo` 하나뿐이다.

이 마지막 사실이 중요하다. React로 가면 **진짜 URL 라우팅이 공짜로 생긴다.**
레시피 상세가 링크로 공유되고, 뒤로가기가 동작하고, 검색엔진에 잡힌다. 지금은
셋 다 안 된다. 런칭을 목표로 한다면 이건 부가 효과가 아니라 핵심 이득이다.

### 4번 회원·로그인 개편 — 지금이 유일한 적기

현재 `users` 43행 중 36개가 username이 NULL이고, 이름 있는 계정은 전부
검증용(`testuser01`, `prod_verify_20260721` 등)이거나 본인 계정이다. **버려서
아까울 계정이 하나도 없다.**

즉 스키마를 갈아엎는 비용이 지금은 0이고, 실사용자가 한 명이라도 생기는 순간
마이그레이션 부담이 생긴다. 순서상 반드시 런칭 전에 끝내야 한다.

개편에 포함할 것은 세 가지다. 토큰 방식 재설계(현재는 단순 토큰, 만료·갱신 개념이
약하다), 계정 데이터 정리(테스트 계정 폐기 + username NOT NULL), 그리고
**개인정보 수집·이용 동의 절차**다. 세 번째는 선택이 아니다 — 알레르기와
건강목표를 저장하는데 지금은 처리방침도 동의 절차도 없다.

### 3번 디자인 개편 — 여기가 가장 위험하다

두 가지를 짚어야 한다.

**첫째, "완벽한"에는 종료 조건이 없다.** 이 표현대로 진행하면 V3 기간의 대부분을
여기서 쓰게 된다. 무엇이 되면 끝인지 먼저 정의해야 한다.

**둘째, 사용자 없이 하는 디자인 판단은 이미 한 번 빗나갔다.** V2에서 P0~P2
접근성·일관성 개선을 다 했는데 "별로 크게 달라진 게 없는 것 같다"는 반응이
나왔고, 실제로 인상을 바꾼 건 그 뒤에 한 액센트 색·아이콘·카드 레이아웃이었다.
같은 실수를 반복할 위험이 있다.

**다만 "테스트 전에 다듬고 싶다"는 판단 자체는 옳다.** 지인 5명에게 보여줄 첫인상은
한 번뿐이고, 조악한 화면을 보여주면 핵심 가치가 아니라 겉모습에 대한 피드백만
돌아온다. 그래서 이 계획은 **디자인을 건너뛰지 않되 "완벽" 대신 종료 조건이 있는
기준으로 바꾼다** (아래 4절).

### 1번 보고서 개선사항 — React 전환으로 자동 해결되는 것을 갈라내야 한다

전부를 V3 작업으로 잡으면 중복이 생긴다.

| 보고서 항목 | React 전환 시 |
|---|---|
| 프론트 테스트 0% | **자동 해결** — 처음부터 Vitest·Playwright로 짜면 된다 |
| Reflex 상태 락 / 로그인 순차 호출 | **자동 해결** — 클라이언트에서 병렬 요청 |
| 호스팅 3분할 | **자동 해결** — Next.js + FastAPI 2개로 축소 |
| 추천 웜 응답 2.6초 | **그대로 남음** (백엔드) |
| admin 라우터 테스트 없음 | **그대로 남음** (백엔드) |
| CI가 sqlite로만 검증 | **그대로 남음** (백엔드) |
| 죽은 코드 3개 | **그대로 남음** |
| 백업·모니터링·개인정보 없음 | **그대로 남음, 런칭 시 필수화** |

### 5번 런칭 — 고정비 결정이 포함된다

런칭은 코드 작업이 아니라 **돈과 약관의 문제**다. 오늘 확인한 현재 정책 기준이다.

**Vercel Hobby는 비상업적·개인 용도로 제한된다.** 광고·결제·유료 요소가 붙으면
약관 위반이며 Vercel이 실제로 제재한 사례가 있다. 무료로 서비스만 한다면 회색지대에
머물 수 있지만, 수익화를 조금이라도 붙일 계획이면 Pro(월 $20)가 필요하다.

**Render 무료 티어는 15분 유휴 후 중단되고 콜드스타트가 30~60초다**(V2에서 실측
36.3초). 런칭한 서비스에 첫 방문자가 36초를 기다리는 건 사실상 실패다. Starter는
서비스당 월 $7이다.

정리하면 런칭 시나리오별 월 고정비는 이렇다.

| 시나리오 | 구성 | 월 비용 |
|---|---|---|
| 지인 테스트만 | 전부 무료 유지 | $0 (콜드 36초 감수) |
| 무료 공개 서비스 | Render Starter ×2 | **$14** |
| 수익화 포함 | 위 + Vercel Pro | **$34** |

**이 결정을 먼저 해야 한다.** "런칭한다"고 정해놓고 무료 티어에 남으면 콜드스타트가
모든 노력을 무효화한다.

---

## 2. 결정이 필요한 사항

착수 전에 정해야 할 것 네 가지다. 각각 추천안을 함께 적었다.

### 2.1 프레임워크 — Next.js(App Router) 추천

레시피 상세 페이지가 검색엔진에 잡히고 링크로 공유되는 게 이 서비스에 실질적
이득이라 SSR이 있는 쪽이 낫다. Vercel 네이티브라 배포도 단순해지고, PWA도 유지
가능하다.

대안은 Vite + React(SPA)다. 더 가볍고 빌드가 빠르지만 SEO를 포기하게 된다.
학습 목적이 우선이라면 이쪽도 합리적이다.

### 2.2 저장소 — 같은 저장소에 `web/` 디렉터리 추천

백엔드를 공유하므로 모노레포가 자연스럽다. CI 설정과 이슈 히스토리도 이어진다.
V2 프론트(`naengjango_v2/`)는 지우지 않고 남겨두면 이식 중 참조할 수 있다.

### 2.3 런칭 고정비 상한

위 표에서 하나를 고른다. 이 선택이 4절의 일정과 5절의 체크리스트를 좌우한다.

### 2.4 V2 프로덕션 처리 — 살려두기 추천

V3가 뜰 때까지 현재 Vercel + Render 배포를 그대로 둔다. 비용이 0이고, V3에서 문제가
생겼을 때 되돌아갈 곳이 된다. Reflex Cloud의 구버전 URL은 혼동을 막기 위해 정리한다.

---

## 3. "디자인 완료"의 종료 조건

3번 항목이 무한정 늘어지지 않도록, 아래를 전부 만족하면 완료로 본다.

1. **디자인 토큰이 코드로 존재한다** — 색·타이포·간격·radius·그림자가 변수로
   정의되고, 하드코딩된 값이 없다
2. **핵심 화면 5개의 시안이 확정됐다** — 홈, 냉장고, 추천 결과, 레시피 상세,
   로그인. 나머지 화면은 이 다섯의 조합으로 파생시킨다
3. **다크 모드가 두 테마 모두 검증됐다**
4. **Lighthouse 접근성 90점 이상** — V2에서 한 번도 측정하지 않았다
5. **명암비 WCAG AA 통과** — 액센트 색 교체 시 검증하지 않았던 항목

"더 예뻐 보일 때까지"가 아니라 이 다섯 개가 기준이다. 통과하면 다음 단계로 넘어간다.

---

## 4. 단계 계획

순서에 한 가지 중요한 변경이 있다. **사용자가 적은 순서(React → 디자인)가 아니라
디자인을 React 착수 전에 확정한다.** 컴포넌트를 만들고 나서 디자인을 바꾸면 같은
것을 두 번 만들게 된다.

### Phase 1 — 백엔드 정비 + 인증 개편

React가 소비할 API를 먼저 안정시킨다. 느리고 검증 안 된 API 위에 프론트를 올리면
우회 코드를 심게 되고, 그게 나중에 부채가 된다.

- 추천 웜 응답 2.6초 → 1초 미만. **먼저 `EXPLAIN ANALYZE`로 후보 조회와 점수
  계산 중 어디가 병목인지 갈라낸 뒤** 고친다. 보류해둔 IN절→JOIN 리팩터가 여기 걸린다
- 회원·로그인 개편: 토큰 재설계, 계정 데이터 정리, 개인정보 동의 절차
- admin 라우터 테스트 (권한 우회 시나리오 중심)
- CI에 Postgres 서비스 컨테이너 추가
- 죽은 코드 3개 정리 (`notify_agent`, `clean_recipe_tags`, `recommendation_graph`)

`recommendation_graph`는 지우기 전에 판단이 필요하다. 살린다면 전체 추천을 LLM으로
바꾸는 게 아니라 **상위 5개의 추천 이유 문장만 생성**하는 방식이 현실적이다.

### Phase 2 — 디자인 확정 (코드 아님)

3절의 다섯 조건을 채운다. 토큰과 시안까지만 하고 구현은 하지 않는다.

### Phase 3 — React 구현

- 프로젝트 구성, 디자인 토큰 적용, 인증 흐름
- 화면 이식 — V2의 단일 페이지를 **실제 라우트로 분해**한다
- 테스트를 코드와 같은 속도로 작성한다. 나중에 몰아 쓰면 V2와 똑같이 절반이 빈다
- 사용 로그 수집을 이때 넣는다. 추천 호출·상세 열람만 남겨도 다음 개선의 근거가 된다

### Phase 4 — 지인 5명 테스트

**여기가 계획에 추가한 체크포인트다.** 3번 항목의 취지(조악한 상태로 보여주지 않기)는
Phase 2~3에서 이미 충족된다. 런칭 직전에 5명에게 일주일 써보게 하고, DB에서 재료
입력 수·추천 호출 수·이탈 지점을 확인한다.

**이 데이터 없이 런칭하면 V2의 실수를 그대로 반복하게 된다.** V2는 84개 기능을
만들고 단 한 명에게도 검증받지 않았다.

### Phase 5 — 런칭 준비

2.3에서 정한 고정비에 따라 실행한다.

- 콜드스타트 대응 (Render Starter 전환 또는 유지 결정)
- 개인정보 처리방침 페이지
- 에러 모니터링 (Sentry 무료 플랜, 연동 1시간)
- DB 백업 (`pg_dump` 주기 실행 + 별도 보관)
- 공공데이터 이용약관 개별 확인 (수익화 시 필수)
- LICENSE 파일

---

## 5. 이 계획에서 의도적으로 뺀 것

**새 기능.** V3는 재구축과 런칭이지 기능 확장이 아니다. V2에 이미 53개 엔드포인트와
화면 81개분이 있고, 그중 무엇이 쓸모 있는지 아직 아무도 모른다. **Phase 4에서
데이터가 나오기 전까지 기능을 추가하지 않는다.**

**제휴 수익 연동.** 쿠팡파트너스는 승인 후 Deeplink API(HMAC 서명)를 거쳐야 수수료가
잡힌다. 코드 문제가 아니라 사업자 승인 문제라 런칭 이후로 미룬다.

**추천 알고리즘 개정.** V2에서 9차까지 개정했지만 전부 개발자 판단이었다. Phase 4
데이터가 나온 뒤에 손대는 게 맞다.

---

*근거: V2 실측치는 `PROJECT_REPORT_2026-08-12.md`. Vercel·Render 요금과 약관은
2026-08-12 확인 기준이며 정책은 변경될 수 있다.*
